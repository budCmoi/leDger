services:
  mongo:
    image: mongo:7.0
    restart: unless-stopped
    ports:
      - '27017:27017'
    environment:
      MONGO_INITDB_DATABASE: ledger-premium-saas
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: